[5.1]

Si consideri il seguente algoritmo, che lavori in fasi t = 1,2, ....
Sia G0 = (N,E,w) il sottografo di G tale che E0 = {e ∈ E: w (e) ≥ 0}, ovvero, tale che G0 abbia gli stessi vertici di G r contenga solo i bordi di G di peso non-negativo.
Per ogni t ≥ 1, sia Gt = (Nt,Et,w) il grafo ottenuto dopo la fase t.
In qualsiasi fase t≥1, una nuova coalizione isomorfa a K2 viene aggiunta a C come segue : Sia et−1 = {i, j} l'arco in Et-1 di peso massimo w i,j = max e∈Et−1 w e.
Aggiungiamo a C la coalizione formata da i e j, cioè C = C ∪ {i, j}.
Inoltre, sia Gt tale che Nt = Nt-1 \ {i,j} e Et ⊂ Et−1 il sottoinsieme di archi di G0 indotto dai nodi Nt.
Quando Et = ∅, l'algoritmo termina ritornando C ∪ {{i}|i ∈ Nt }.
Poichè ad ogni fase viene rimosso almeno un arco dal grafo, gli algoritmi terminano al massimo in |E| fasi ritornando un risultato con tutte le coalizione di cardinalità al massimo di 2.
Per prima cosa dimostriamo che C è un Core Stable Outcome di G(G).
Ricordiamo che, per ogni possibile risultato ui (C) ≤ δimax.
Pertanto, nell'esito C, gli agenti i e j selezionati nella fase t=1 stanno raggiungendo la massimo utilità che possono sperare di raggiungere.
Ciò implica che tali agenti non possono appartendere a nessuna coalizoine fortemente bloccata.
La dimostrazione continua per induzione come segue.
Supponiamo che tutti gli agenti selezionati fino alla fase q, cioè gli agenti appartenenti a N \ Nq non possano appartenere a nessuna coalizione fortemente bloccata, quindi gli agenti iq+1 e jq+1 selezionati nella fase q+1 non possono appartenere a nessuna coalizione fortemente bloccata come sopra.
In effetti, supponiamo che tali agenti abbiano una certa utilità x nella coalizione C.
Per l'ipotesi induttiva abbiamo che possono creare una coalizione fortemente bloccata solo con agenti appartenenti a Nq+1.
Tuttavia, poichè l'arco (iq+1, jq+1) ha i pesi massimo in Gq+1, implica che non possono ottenere un'utilità maggiore di x.
Infine, per gli agenti che non sono eguagliati, cioè gli agenti che sono soli in una coalizione, dal momento che formano un insieme indipendente, non possono formare una coalizione fortemente bloccata, e questo fornisce la dimostrazione.
Resta da mostrare che SW(C) ≥ 1/2 SW(C∗(G(G))).
Prima di tutto si noti che in ogni fase t, una coalizione che contribuisce 2wet−1 al benessere sociale viene aggiunta a C; noi otteniamo così che SW(C) = sommatoria t≥1 2wet−1 .
Per ogni e ∈ E, sia f(e,i) ∈ {0,1,2} il numero di endpoints di e appartenenti alla coalizione C*i.
E' possibile limitare SW(C*(G(G))) come segue 
(4)
(5)
dove la disuguaglianza 4 vale perchè w et−1 = max e∈Et−1 w e ogni endpoint di e t-1 appartenente a C*i può avere al massimo |C*i-1| archi adiacenti (notiamo che tutti gli archi in Et \ Et-1 sono adiacenti agli endpoint di e t-1), e l'equazione 5 vale poichè, dato che C*1, ... (l puntini) , C*n son una partizione di N, segue per definizione di f che sommatoria C*i∈C* f(e t-1,i) = 2.
Perciò, (6)

[5.3]
Si consideri il grafo G rappresentato in FIGURA 6
FIGURA 6
Da un lato, è facile verificare che l'unico Core Stable clustering di C sia quello in cui i 2 agenti centragli i2 e i3 sono insieme nella stessa coalizione, mentre l'agente i1, così come l'agente i4, sono soli in diverse coalizioni, cioè C = {{i1}, {i2,i3}, {x4}}.
Si noti che SW(C) = 2(1 + ϵ 2).
Dall'altra parte, il risultato C′ = {{i1,i2}, {i3,i4}}, ha un benessere sociale uguale a 4, e quindi SW(C∗) ≥ 4.
Segue che CPoS(G(G)) ≥ 4 / 2(1+ϵ/2) ≥ 2 − ϵ.

[5.5]
Sia C' la soluzione calcolata dal Teorema 5.1, in cui tutte le coalizinoi hanno al massimo cardinalità 2.
Si consideri qualsiasi Core Stable Outcome C.
Per ogni coalizione C′ = {i, j} di C' isomorfa a K2, da un lato abbiamo che SW(C′) = 2.
Dall'altro , poichè C è un Core Stable Outcome, ui (C) = 1 oruj (C) = 1, perchè altrimenti la coalizinoe {i,j} bloccherebbe fortemente l'esito C.
Quindi ui (C) + uj (C) ≥ 1, mentre ui (C′) +uj (C′) = 2.
Sia N '⊆ N tale che per ogni j ∈ N', C'J è isomorfo a K2.
Poichè gli angeti che si trovano in tutte le altre coalizioni di C' non contribuiscono a SW(C'), otteniamo 
(7)
La prova segue poichè, dal Lemma 5.1, SW(C∗(G(G))) ≤ 2 · SW(C′).

[5.6]
Per il limite inferiore, è facile vedere che, dato un percorso non-pesato di 4 nodi i1,i2,i3,i4, l'esito in cui tutti gli agenti appartenfono alla stessa coalizione è Core Stable e ha benessere sociale 2, mentre il risultato ottimo C∗ = {{i1,i2}, {i3,i4}} ha benessere sociale 4.
Un limite superiore corrispondente può essere ottenuto sfruttando gli stessi argomenti usati nella dimostrazione del Teorema 3.9.